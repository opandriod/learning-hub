Mobile navigation fix included.

What changed:
- Mobile sidebar no longer pushes the whole page down by default.
- Mobile now shows a compact top header with Learning Hub + menu button.
- Search, menu links, and logout appear only when the menu is opened.
- Menu text wraps instead of being cut off.
- Main content starts near the top on mobile.
- Ask AI button is smaller and less blocking on mobile.

Test:
1. Open site on mobile width.
2. Dashboard/content should appear immediately.
3. Tap the menu button to see navigation and logout.
4. Tap any menu link; menu should close automatically.
