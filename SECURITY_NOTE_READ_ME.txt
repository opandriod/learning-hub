IMPORTANT SECURITY NOTE

This ZIP includes environment files using the Supabase/database values you provided.

Do not upload this ZIP publicly or commit these .env files to GitHub.
The frontend only uses the publishable/anon Supabase key.
The database password and service role key are backend-only secrets.

Because these values were shared in chat, you should rotate/regenerate them in Supabase before final deployment or submission if the project will be public.
